services:
  - type: static
    name: karibu-padel
    envVars:
      - key: NODE_VERSION
        value: '20'
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
